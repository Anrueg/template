$schema: "https://moonrepo.dev/schemas/tasks.json"

tasks:
  start:
    extends: build
    args: [--watch]
    preset: server
    deps:
      - ^:start
      - ~:start.pre
    options:
      cache: true
