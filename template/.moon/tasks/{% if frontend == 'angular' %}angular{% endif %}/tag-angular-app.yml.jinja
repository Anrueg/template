$schema: "https://moonrepo.dev/schemas/tasks.json"

tasks:
  start:
    command: ng serve $projectName
    preset: server
    toolchain: {{ frontend_toolchain }}
    options:
      runFromWorkspaceRoot: true
