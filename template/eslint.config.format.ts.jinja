// @ts-expect-error no type definition for this module
import * as onlyWarn from "eslint-plugin-only-warn"

import base from "./eslint.config"

/* ! DO NOT EDIT ! */

export default [
    ...base,
    {
        name: "Only Warnings (Use for format only)",
        plugins: {
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            "only-warn": onlyWarn
        }
    }
].filter((v, i, a) => a.findIndex(value => value.name === v.name) === i)
